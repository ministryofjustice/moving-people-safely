p
  | There is no previously PER record on
  |  Moving People Safely for prison number
  |  #{prison_number}

= form_for Escort.new(prison_number: prison_number), url: escorts_path, method: :post do |f|
  = f.hidden_field :prison_number
  .form-actions
    = f.submit 'Create new profile', class: 'button'
