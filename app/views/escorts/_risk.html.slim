#risk
  = render partial: 'status',
    locals: { workflow: risk, not_started_path: escort_risks_path(escort), summary_path: summary_escort_risks_path(escort) }

  h2
    ' Risk
    / small 16:44 | 04 Apr 2016
  - unless risk.no_questions_answered?
    .info-table.info-table-big
      .grid-row
        .column-one-sixth.answered_yes
          small Risk
          p = risk.total_questions_with_relevant_answer
        .column-one-sixth.answered_no
          small No risk
          p = risk.total_questions_without_relevance
        - unless risk.all_questions_answered?
          .column-one-sixth.text-error.not_answered
            small Missing
            p = risk.total_questions_not_answered
  - else
    hr
