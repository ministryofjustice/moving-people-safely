header
  h1
    = escort.prison_number
    ' :
    = escort.surname
    span.heading-light<
      = escort.forenames

- escort.alerts.each do |alert, on|
  - status = on ? 'active' : 'inactive'

  li class="flag-#{status}" id="#{alert}#{'-inactive' if status == 'inactive' && alert == :csra}"
    = t("escort.#{status}_alerts.#{alert}")
