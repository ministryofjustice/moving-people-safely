header#header
  h1
    ' #{detainee.prison_number}: #{detainee.surname}, #{detainee.forenames}
table.detainee
  tr
    - if detainee.image_filename.present?
      td rowspan=2 class='image'
        = image_tag detainee.image_filename, size: '128x180'

    td
      span#not_for_release_header = image_tag "not_for_release_#{offences.not_for_release}.png"
      span.text = offences.not_for_release_details

    td
      span#csra_header = image_tag "csra_#{risk.csra}.png"

    td
      span#acct_status_header = image_tag "#{acct_status_image(risk.acct_status)}.png"

  tr
    td
      span#rule_45_header = image_tag "rule_45_#{risk.rule_45}.png"

    td
      span#category_a_header = image_tag "category_a_#{risk.category_a}.png"
      span.text = risk.category_a_details

    td
